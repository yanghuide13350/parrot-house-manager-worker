{
  "name": "parrot-pro-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply parrot-pro-dev --local",
    "db:migrate:remote": "wrangler d1 migrations apply parrot-pro-dev --remote"
  },
  "devDependencies": {
    "wrangler": "4.26.0"
  }
}
